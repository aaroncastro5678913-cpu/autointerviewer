# Backend URL used by the Vite dev proxy. The browser always calls /api/...,
# which the dev server forwards here. In production, serve the frontend behind
# a reverse proxy that routes /api to the backend (see README).
VITE_BACKEND_URL=http://localhost:4000
